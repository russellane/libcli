"""Base class for our `Actions`s."""

import argparse


class BaseAction(argparse.Action):
    # pylint: disable=abstract-method
    # pylint: disable=too-few-public-methods
    """Base class for our `Actions`s."""
