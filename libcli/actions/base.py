"""Base class for our `Actions`s."""

import argparse


class BaseAction(argparse._HelpAction):
    # pylint: disable=protected-access
    # pylint: disable=too-few-public-methods
    """Base class for our `Actions`s."""
